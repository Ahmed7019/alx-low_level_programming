#include <unistd.h>
#include <ctype.h>
/**
 * _print_alphabet - prints the alphabet in lower case
 * @a: The alphapet to print
 *
 * Return: on success 0.
 */

void print_alphapet(void)
{
	char a;
	putchar('%c', tolower(a));

	return (0);
}
